import CreateBranchScreen from './create-branch'

export default function EditBranchScreen() {
  // This screen uses the same component as create-branch
  // The create-branch screen handles both create and edit based on params
  return <CreateBranchScreen />
}

